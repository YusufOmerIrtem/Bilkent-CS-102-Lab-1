import java.util.Random;

public class Map {
    // constructer
    public Map( int players ) {
        // initialise map base
        gameMap = new GameContent[11][11];

        // initialise players
        if( players == 2 ) {

        }
        else if(players == 3 ) {

        }
        else {

        }

        // initialise unpassable blocks
        if( players == 2 ) {

        }
        else if(players == 3 ) {

        }
        else {

        }

        // initialise pickable objects


        // initialise unoccipied cells

    }

    // variables
    GameContent[][] gameMap;
    Random contentNumber = new Random();

    // methods
}
